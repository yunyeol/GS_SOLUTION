<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="gs.msg.target.mapper">

    <select id="selectData" resultType="Map">
        SELECT MIN(ID) MIN_ID,
                MAX(ID) MAX_ID
        FROM test
        WHERE readflag ='N'
    </select>

    <select id="selectData2" resultType="testDto" parameterType="Map">
        SELECT
              id id,
              TEST test
        FROM test
        WHERE id BETWEEN #{minValue} AND #{maxValue}
        AND readflag ='N'
    </select>

    <update id="insertData" parameterType="testDto">
        UPDATE test SET readflag = 'Y' WHERE ID = #{id}
    </update>

</mapper>

